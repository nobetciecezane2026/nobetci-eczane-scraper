{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port ${PORT}"
  },
  "crons": [
    {
      "schedule": "0 4 * * *",
      "command": "python scraper.py"
    }
  ]
}
